spring.boot:
  admin:
    client:
      instance:
        health-url: http://localhost:38080/swagger-ui.html
      #url: http://localhost:8091
      auto-registration: true
management:
  endpoint:
    health:
      show-details: ALWAYS
  endpoints:
    web:
      exposure:
        include: "*"